{
  "name": "dream",
  "version": "0.0.0",
  "description": "dream orm",
  "main": "dist/src/index.js",
  "repository": "https://github.com/avocadojesus/dream.git",
  "author": "avocadojesust",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc",
    "sync": "CORE_DEVELOPMENT=1 ts-node boot/sync.ts",
    "copy-models": "CORE_DEVELOPMENT=1 ts-node src/bin/copy-models.ts",
    "db:migrate": "yarn sync && ALREADY_AT_PROJECT_ROOT=1 ts-node src/bin/migrate.ts && yarn copy-models && yarn build && yarn sync",
    "db:drop": "ts-node src/bin/db-drop.ts",
    "db:create": "ts-node src/bin/db-create.ts",
    "spec": "CORE_DEVELOPMENT=1 jest --runInBand --forceExit",
    "console": "yarn build && CORE_DEVELOPMENT=1 NODE_ENV=development ts-node --project ./tsconfig.json ./src/test-app/conf/repl.ts",
    "dream": "ts-node ./boot/cli.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/jest-plugin-context": "^2.9.5",
    "@types/luxon": "^3.3.0",
    "jest": "^29.5.0",
    "jest-plugin-context": "^2.9.0",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@types/pg": "^8.6.6",
    "@types/pluralize": "^0.0.29",
    "commander": "^10.0.0",
    "dotenv": "^16.0.3",
    "kysely": "^0.24.2",
    "kysely-codegen": "^0.10.0",
    "luxon": "^3.3.0",
    "pg": "^8.10.0",
    "pluralize": "^8.0.0",
    "yaml": "^2.2.1"
  }
}
