{
  "name": "dream",
  "version": "0.0.0",
  "description": "dream orm",
  "main": "dist/src/index.js",
  "repository": "https://github.com/avocadojesus/dream.git",
  "author": "avocadojesus <jgarbey@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc",
    "sync": "yarn build && CORE_DEVELOPMENT=1 ts-node src/bin/sync.ts",
    "enhance-schema": "yarn build && CORE_DEVELOPMENT=1 ts-node src/bin/enhanceSchema.ts",
    "db:migrate": "yarn sync && node dist/src/bin/migrate.js",
    "spec": "CORE_DEVELOPMENT=1 jest --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.5.0",
    "jest-plugin-context": "^2.9.0",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "@types/pg": "^8.6.6",
    "@types/pluralize": "^0.0.29",
    "dotenv": "^16.0.3",
    "kysely": "^0.24.2",
    "kysely-codegen": "^0.10.0",
    "pg": "^8.10.0",
    "pluralize": "^8.0.0",
    "yaml": "^2.2.1"
  }
}
